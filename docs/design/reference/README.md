# Reference screenshot — missing

The dashboard reference screenshot belongs in this directory. **It has not been supplied.**

Per spec §0, the owner places it here, and if it is missing it should be asked for before M1 design work
begins. `docs/DESIGN-DIRECTION.md` was written from the spec's written description of the reference instead,
and should be reviewed against the real screenshot before it is approved.

## What it is for

**Visual direction only.** Per §7.1, its logos, text, assets, colours and pixel layout are not copied. What is
taken from it is the overall feel: a dark compact navigation area, a large bright workspace, rounded
components, soft borders, restrained shadows, small status indicators, strong type hierarchy, generous
whitespace.

Two constraints apply regardless of what the screenshot shows:

- **No violet or purple as the primary colour** — it evokes the upstream provider's identity.
- The result must look like WebEdge, not like the reference and not like a template (§7.10).

## Also needed

Brand assets, if they exist (§2.3): logo (light and dark), logo mark, favicon, and any existing brand colours.
If WebEdge has brand assets, the design tokens are derived from them rather than from the proposal in §7.2.
